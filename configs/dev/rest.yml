server:
  port: 8080
spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luogen-at/config-server
          skip-ssl-validation: true
          search-paths: configs
